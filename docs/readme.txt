To build the docs locally:

- Make a local checkout of https://github.com/Deltares/hydromt_sfincs

- Open anaconda prompt

- cd to the right folder (d:\repos\SFINCS_docs\docs\)

- do: 'make html'

> The HTML pages are in _build\html.